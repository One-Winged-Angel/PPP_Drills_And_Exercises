#include "../std_lib_facilities.h"
using namespace std;

int main()
{
	vector <double> temps; // declare a vector to hold the data
	for (double temp; cin >> temp;) //this line reads in a temp
	{
		temps.push_back(temp); //this line puts the temp into the vector
	}
	//essentially as long as we continue to read doubles into the vector they 
	// will be added and this will only stop after we stop feeding in valid inputs i.e. doubles





	return 0;
}